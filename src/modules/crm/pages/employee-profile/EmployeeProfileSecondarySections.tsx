import { Button } from "@components/ui/Button";
import { Card } from "@components/ui/Card";
import { Input } from "@components/ui/Input";
import { fmtDate } from "./shared";

export function EmployeeProfileSecondarySections(props: any) {
  const { tab, busy, activities, sessions, loadActivities, loadSessions, selectedLead, setSelectedLead, requestOpen, setRequestOpen, selectedRequest, reviewNote, setReviewNote, newPassword, setNewPassword, emailOtp, setEmailOtp, decisionBusy, verifyEmailOtp, decide } = props;
  return (
    <>
      {selectedLead ? <div className="fixed inset-0 z-[999] flex items-center justify-center bg-slate-900/40 p-4 backdrop-blur-sm" onClick={() => setSelectedLead(null)}><div className="w-full max-w-lg rounded-[5px] border border-slate-100 bg-white shadow-2xl" onClick={(e) => e.stopPropagation()}><div className="p-4 border-b border-slate-100 bg-slate-50 flex items-center justify-between"><div><div className="text-[10px] font-black uppercase tracking-widest text-slate-500">Lead</div><div className="mt-1 text-lg font-black text-slate-900">{selectedLead.phone}</div></div><Button variant="ghost" onClick={() => setSelectedLead(null)}>Close</Button></div><div className="p-4 grid gap-2 text-[13px]"><div className="flex items-center justify-between"><span className="font-semibold text-slate-500">Status</span><span className="font-black text-slate-900">{selectedLead.status}</span></div><div className="flex items-center justify-between"><span className="font-semibold text-slate-500">Assigned</span><span className="font-semibold text-slate-700">{fmtDate(selectedLead.assignedAt)}</span></div><div className="flex items-center justify-between"><span className="font-semibold text-slate-500">First inbound</span><span className="font-semibold text-slate-700">{fmtDate(selectedLead.firstInboundAt)}</span></div><div className="flex items-center justify-between"><span className="font-semibold text-slate-500">Last inbound</span><span className="font-semibold text-slate-700">{fmtDate(selectedLead.lastInboundAt)}</span></div><div className="flex items-center justify-between"><span className="font-semibold text-slate-500">Created</span><span className="font-semibold text-slate-700">{fmtDate(selectedLead.createdAt)}</span></div><div className="flex items-center justify-between"><span className="font-semibold text-slate-500">Updated</span><span className="font-semibold text-slate-700">{fmtDate(selectedLead.updatedAt)}</span></div></div></div></div> : null}
      {requestOpen && selectedRequest ? <div className="fixed inset-0 z-[999] flex items-center justify-center bg-slate-900/40 p-4 backdrop-blur-sm" onClick={() => setRequestOpen(false)}><div className="w-full max-w-2xl rounded-[5px] border border-slate-100 bg-white shadow-2xl" onClick={(e) => e.stopPropagation()}><div className="p-4 border-b border-slate-100 bg-slate-50 flex items-center justify-between"><div><div className="text-[10px] font-black uppercase tracking-widest text-slate-500">Request</div><div className="mt-1 text-lg font-black text-slate-900">{String(selectedRequest.metadata?.requestType || "-")}</div><div className="text-[11px] font-semibold text-slate-500">Status: {String(selectedRequest.metadata?.status || "-")}</div></div><Button variant="ghost" onClick={() => setRequestOpen(false)}>Close</Button></div><div className="p-4 grid gap-3"><div className="grid gap-2 text-[13px]"><div className="flex items-center justify-between"><span className="font-semibold text-slate-500">Employee</span><span className="font-black text-slate-900">{String(selectedRequest.metadata?.employeeEmail || "-")}</span></div><div className="flex items-center justify-between"><span className="font-semibold text-slate-500">Requested Name</span><span className="font-semibold text-slate-700">{String(selectedRequest.metadata?.requestedName || "-")}</span></div><div className="flex items-center justify-between"><span className="font-semibold text-slate-500">Requested Email</span><span className="font-semibold text-slate-700">{String(selectedRequest.metadata?.requestedEmail || "-")}</span></div><div className="flex items-center justify-between"><span className="font-semibold text-slate-500">Reason</span><span className="font-semibold text-slate-700">{String(selectedRequest.metadata?.reason || "-")}</span></div></div><Input label="Review note (optional)" value={reviewNote} onChange={(e) => setReviewNote(e.target.value)} />{String(selectedRequest.metadata?.requestType || "") === "password_reset" ? <Input label="Set new password (optional). Leave empty to send reset link." type="password" value={newPassword} onChange={(e) => setNewPassword(e.target.value)} /> : null}{String(selectedRequest.metadata?.requestType || "") === "change_email" ? <div className="grid gap-2"><Input label="Email OTP (after approval)" value={emailOtp} onChange={(e) => setEmailOtp(e.target.value.replace(/[^\d]/g, "").slice(0, 6))} placeholder="123456" /><Button variant="outline" disabled={decisionBusy || !emailOtp} onClick={verifyEmailOtp}>{decisionBusy ? "Verifying..." : "Verify OTP & Apply Email Change"}</Button></div> : null}<div className="flex items-center justify-end gap-2 pt-2 border-t border-slate-100"><Button variant="ghost" disabled={decisionBusy} onClick={() => decide("rejected")}>Reject</Button><Button disabled={decisionBusy} onClick={() => decide("approved")}>{decisionBusy ? "Please wait..." : "Approve"}</Button></div></div></div></div> : null}
      {tab === "activities" ? <Card className="border-slate-200 shadow-sm rounded-[5px] overflow-hidden"><div className="p-4 border-b border-slate-100 flex items-center justify-between"><div><div className="text-sm font-black text-slate-900">Activities</div><div className="text-[11px] font-semibold text-slate-500">Assignment audits (latest first).</div></div><Button variant="ghost" disabled={busy} onClick={loadActivities}>Refresh</Button></div><div className="overflow-x-auto"><table className="min-w-[900px] w-full text-[12px]"><thead className="bg-slate-50 text-slate-600"><tr className="text-left"><th className="px-4 py-3 font-black uppercase tracking-widest text-[10px]">Time</th><th className="px-4 py-3 font-black uppercase tracking-widest text-[10px]">Phone</th><th className="px-4 py-3 font-black uppercase tracking-widest text-[10px]">Mode</th><th className="px-4 py-3 font-black uppercase tracking-widest text-[10px]">Reason</th></tr></thead><tbody>{(activities?.items || []).map((a: any) => <tr key={a.id} className="border-t border-slate-100 hover:bg-slate-50/60"><td className="px-4 py-3 text-slate-600 font-semibold">{fmtDate(a.createdAt)}</td><td className="px-4 py-3 font-black text-slate-900">{a.phone}</td><td className="px-4 py-3 text-slate-600 font-semibold">{a.mode || "-"}</td><td className="px-4 py-3 text-slate-600 font-semibold">{a.reason || "-"}</td></tr>)}{!busy && (activities?.items || []).length === 0 ? <tr><td colSpan={4} className="px-4 py-10 text-center text-slate-500 font-semibold">No activities found.</td></tr> : null}</tbody></table></div></Card> : null}
      {tab === "sessions" ? <Card className="border-slate-200 shadow-sm rounded-[5px] overflow-hidden"><div className="p-4 border-b border-slate-100 flex items-center justify-between"><div><div className="text-sm font-black text-slate-900">Login / Logout</div><div className="text-[11px] font-semibold text-slate-500">Events recorded when employee signs in/out.</div></div><Button variant="ghost" disabled={busy} onClick={loadSessions}>Refresh</Button></div><div className="overflow-x-auto"><table className="min-w-[900px] w-full text-[12px]"><thead className="bg-slate-50 text-slate-600"><tr className="text-left"><th className="px-4 py-3 font-black uppercase tracking-widest text-[10px]">Time</th><th className="px-4 py-3 font-black uppercase tracking-widest text-[10px]">Type</th><th className="px-4 py-3 font-black uppercase tracking-widest text-[10px]">IP</th><th className="px-4 py-3 font-black uppercase tracking-widest text-[10px]">User agent</th></tr></thead><tbody>{(sessions?.items || []).map((s: any) => <tr key={s.id} className="border-t border-slate-100 hover:bg-slate-50/60"><td className="px-4 py-3 text-slate-600 font-semibold">{fmtDate(s.createdAt)}</td><td className="px-4 py-3 font-black text-slate-900">{String(s.type || "").toUpperCase()}</td><td className="px-4 py-3 text-slate-600 font-semibold">{s.ip || "-"}</td><td className="px-4 py-3 text-slate-600 font-semibold truncate max-w-[520px]">{s.userAgent || "-"}</td></tr>)}{!busy && (sessions?.items || []).length === 0 ? <tr><td colSpan={4} className="px-4 py-10 text-center text-slate-500 font-semibold">No login/logout events yet.</td></tr> : null}</tbody></table></div></Card> : null}
    </>
  );
}
